<%= form_for(@terminaltype) do |f| %>
  <% if @terminaltype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@terminaltype.errors.count, "error") %> prohibited this terminaltype from being saved:</h2>

      <ul>
      <% @terminaltype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_name %><br>
    <%= f.text_field :type_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
