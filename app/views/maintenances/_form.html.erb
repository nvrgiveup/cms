<%= form_for(@maintenance) do |f| %>
  <% if @maintenance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maintenance.errors.count, "error") %> prohibited this maintenance from being saved:</h2>

      <ul>
      <% @maintenance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :saleterminal_id %><br>
    <%= f.number_field :saleterminal_id %>
  </div>
  <div class="field">
    <%= f.label :service_date %><br>
    <%= f.date_select :service_date %>
  </div>
  <div class="field">
    <%= f.label :maint_notes %><br>
    <%= f.text_area :maint_notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
